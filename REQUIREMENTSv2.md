# AI搭載 売上分析ダッシュボード 要件定義書
Version: 2.0

---

# 1. プロジェクト概要

## 1.1 プロジェクト名

AI搭載 売上分析ダッシュボード

## 1.2 背景

クライアントはECブランド（アパレル）を運営しており、
毎月Shopifyから売上データをCSV形式で取得し、
Excelを使用して集計・グラフ作成・社内報告を行っている。

現在、この月次報告作業に毎回約3時間かかっている。

作成したレポートは、

- 社長
- マーケティング部長
- 営業担当者5名

が閲覧している。

単なる数値の可視化だけではなく、
経営判断や次の施策につながる分かりやすいレポートが求められている。

## 1.3 目的

Shopifyの売上CSVをアップロードすることで、

- 売上データの保存
- KPIの自動集計
- 前月比較
- グラフによる可視化
- AIによる分析
- AIによる次月アクション提案

までを行えるWebダッシュボードを構築する。

現在約3時間かかっている月次報告作業を、
30分程度まで短縮することを目標とする。

---

# 2. プロジェクトゴール

## ゴール1：月次報告業務の効率化

CSVアップロード後の集計・グラフ作成・分析コメント作成を自動化し、
人が行う作業を大幅に削減する。

## ゴール2：意思決定に使えるレポートの提供

利用者が、

- 現在どうなっているか
- 前月から何が変化したか
- 何に注目すべきか
- 次に何をすべきか

を短時間で把握できる状態を目指す。

---

# 3. 対象ユーザー

## 3.1 利用者

- 社長
- マーケティング部長
- 営業担当者5名

合計7名程度。

## 3.2 閲覧範囲

Phase 1では、全利用者が同一の会社全体ダッシュボードを閲覧する。

営業担当者個人ごとの売上分析や、
担当者別の閲覧制御はPhase 1では行わない。

そのため、CSVに営業担当者を示す列は必要としない。

## 3.3 権限

Phase 1では役職別の詳細な権限制御は行わない。

認証済みユーザーは原則として同一範囲の分析結果を閲覧できるものとする。

---

# 4. 現在の業務フロー

1. Shopifyから売上CSVを取得
2. Excelへ取り込む
3. データを集計
4. グラフを作成
5. 月次レポートを作成
6. 社内へ共有

所要時間：約3時間 / 月

---

# 5. システム導入後の業務フロー

1. Shopifyから当月分の売上CSVを取得
2. 本システムへCSVをアップロード
3. CSV形式・データ内容を検証
4. 当月データをDBへ保存
5. DBに保存された過去データと合わせて集計
6. KPI・前月比較・グラフを生成
7. AIが集計済みデータを分析
8. AIが次月アクションを提案
9. 利用者がダッシュボードを確認

Phase 1ではShopify APIとの直接連携は行わず、
CSVアップロード方式とする。

---

# 6. データ保持方針

## 6.1 CSVの粒度

毎月アップロードするCSVは、
原則として「対象月1か月分の売上データ」とする。

例：

- 2025年9月分
- 2025年10月分
- 2025年11月分

提供されたサンプルCSVには複数月のデータが含まれているが、
本番想定では月単位でアップロードする。

## 6.2 データ永続化

アップロードされた売上データはDBへ保存する。

過去データを保持することで、

- 前月比較
- リピート顧客判定
- 月次推移

を可能にする。

## 6.3 初回導入時

初回導入時に過去データが存在しない場合、

- 前月比較
- リピート率

は算出できない場合がある。

その場合はエラーにはせず、

「比較データなし」
「算出不可」

などの表示とする。

過去月のCSVが提供される場合は、
初期データとして登録可能とする。

---

# 7. CSVデータ仕様

提供された `case8-sales-sample.csv` を
Phase 1における正式な入力フォーマットとして扱う。

使用する列は以下の8列とする。

| 列名 | 内容 | 主な用途 |
|---|---|---|
| order_date | 注文日 | 月次集計・前月比較 |
| customer_id | 顧客ID | リピート率 |
| product_name | 商品名 | 商品別分析 |
| category | 商品カテゴリ | カテゴリ別分析 |
| sku | SKU | SKUランキング |
| quantity | 販売数量 | 販売数量分析 |
| revenue | 売上金額 | 売上 |
| cost | 原価 | 粗利 |

## 7.1 データ前提

Phase 1では以下を前提とする。

- `revenue` は対象行全体の売上金額
- `cost` は対象行の販売数量分を含む合計原価
- `quantity` は販売数量
- `customer_id` は顧客を一意に識別するID
- 金額はすべて同一通貨

サンプルCSVでは、
quantityが増加するとrevenue・costも比例して増加しているため、
costは単価ではなく行単位の合計原価として扱う。

## 7.2 税の扱い

Phase 1では、
`revenue` に格納されている金額をそのまま売上として使用する。

税込・税抜の変換処理は行わない。

実案件ではShopify側の出力仕様を確認し、
税込・税抜の定義を確定する必要がある。

## 7.3 返品・キャンセル・返金

Phase 1では、
提供されたCSV内の `revenue` および `cost` が
返品・キャンセル・返金等を反映済みの確定値であるものとして扱う。

返品・返金データを別途処理する機能は実装対象外とする。

実案件ではShopifyの実CSV仕様を確認する。

---

# 8. CSVアップロード

## 8.1 検証内容

アップロード時に最低限以下を確認する。

- CSVファイルであること
- 必須8列が存在すること
- データが1件以上存在すること
- `order_date` が有効な日付であること
- `quantity` が有効な数値であること
- `revenue` が有効な数値であること
- `cost` が有効な数値であること

検証失敗時は、
原因が分かるエラーメッセージを表示する。

## 8.2 文字コード

Phase 1ではUTF-8を正式対応とする。

Shift-JIS等への対応は、
実運用で必要と判断された場合の拡張候補とする。

## 8.3 customer_idが空の場合

`customer_id` が空のデータは売上・粗利等の集計には含める。

ただし顧客を識別できないため、
リピート率の母数・分子からは除外する。

---

# 9. 再アップロード

同一対象月のCSVがすでに登録されている状態で
再度アップロードされた場合は、

「対象月データを置き換える」

方式とする。

既存の対象月データを削除してから、
新しいCSV内容を登録する。

Phase 1のCSVには注文IDが存在しないため、
行単位での差分マージは行わない。

これにより、
二重登録による売上・リピート率の重複集計を防止する。

---

# 10. KPI要件

## 10.1 最重要KPI

以下3つを最重要KPIとする。

1. 売上
2. 粗利
3. リピート率

ダッシュボード上部など、
最も視認性の高い場所に表示する。

---

## 10.2 売上

対象月の `revenue` の合計。

売上 = SUM(revenue)

---

## 10.3 粗利

対象月の `revenue - cost` の合計。

粗利 = SUM(revenue - cost)

補助情報として粗利率を表示してもよい。

粗利率 = 粗利 ÷ 売上 × 100

---

## 10.4 リピート率

Phase 1では以下を正式定義とする。

「対象月に購入した識別可能なユニーク顧客のうち、
対象月より前にも購入履歴が存在する顧客の割合」

計算式：

リピート率
=
対象月のリピーター顧客数
÷
対象月の識別可能なユニーク購入顧客数
× 100

### リピーター判定

対象月より前の日付で、
同一 `customer_id` の購入履歴がDB内に1件以上存在する場合、
リピーターと判定する。

### customer_idが空の場合

リピート率計算から除外する。

### 過去データが存在しない場合

リピート率は「算出不可」と表示する。

---

# 11. 前月比較

「前月」は、
対象月の1つ前の暦月と定義する。

例：

2025年11月
→ 前月は2025年10月。

直前にアップロードした月ではない。

対象月の1つ前の暦月データがDBに存在しない場合は、

「比較データなし」

と表示する。

比較対象：

- 売上
- 粗利
- リピート率

必要に応じて前月比率または増減額を表示する。

---

# 12. 補助分析

以下を表示する。

- カテゴリ別売上
- SKU別売上TOP10
- 販売数量
- 月次推移

## 12.1 カテゴリ別分析

選択された対象月の `category` ごとに
売上を集計する。

## 12.2 SKU別売上TOP10

選択された対象月のSKUを
売上金額の高い順に並べる。

SKUが10種類未満の場合は、
存在するSKUをすべて表示する。

## 12.3 販売数量

対象月の `quantity` を集計して表示する。

---

# 13. 在庫回転率

ヒアリングでは「あったら嬉しいKPI」として
在庫回転率の要望がある。

しかし、現在提供されているCSVには、

- 現在庫
- 期首在庫
- 期末在庫
- 平均在庫数量
- 平均在庫金額

等が含まれていない。

そのため現在のデータのみでは、
在庫回転率を正確に算出できない。

Phase 1では実装対象外とする。

AIによる推測値も生成しない。

在庫データ連携はPhase 2候補とする。

---

# 14. ダッシュボード

## 14.1 主要表示

画面上部に最低限以下を表示する。

- 売上
- 粗利
- リピート率
- 前月比

## 14.2 グラフ・分析

以下を確認できること。

- 月次売上推移
- 月次粗利推移
- リピート率推移
- カテゴリ別売上
- SKU別売上TOP10
- 販売数量

## 14.3 月選択

ユーザーは保存済みデータから
分析対象月を選択できる。

---

# 15. AI分析機能

## 15.1 目的

AIはKPIを計算するためではなく、
計算済みデータを解釈して、
利用者の意思決定を支援するために使用する。

## 15.2 AIへ渡すデータ

アプリケーション側で計算済みの、

- 当月売上
- 前月売上
- 売上前月比
- 当月粗利
- 前月粗利
- 粗利前月比
- 当月リピート率
- 前月リピート率
- カテゴリ別売上
- SKU別売上TOP10
- 販売数量

を構造化してAIへ渡す。

生CSV全件をそのままAIへ送信する方式は採用しない。

## 15.3 AI分析内容

以下を生成する。

1. 当月サマリー
2. 前月から大きく変化した点
3. 売上への寄与が大きいカテゴリ・SKU
4. 注目すべきポイント
5. 次月のアクション提案

## 15.4 トーン

コンサルタントが経営・マーケティング担当者へ
分析結果を説明するようなトーンとする。

## 15.5 推測表現

データだけでは原因を特定できない場合、

- 「可能性があります」
- 「考えられます」
- 「確認を推奨します」

などを使用し、
推測を事実として断定しない。

---

# 16. AI出力の数値安全性

KPI等の数値はアプリケーション側で計算し、
AIに独自計算させない。

AI分析画面で表示する主要KPI数値は、
アプリケーション側の計算結果を直接表示する。

AIによる文章は補足説明として扱う。

Phase 1では、

「AIが絶対に誤った数値を生成しない」

ことを保証要件とはしない。

代わりに、

「ユーザーが意思決定に使用する正式なKPI数値は、
AI生成値ではなくアプリケーション計算値を使用する」

ことを要件とする。

---

# 17. AI利用量

通常運用は月次レポート作成を想定する。

基本的なAI分析は、

1対象月につき1回の分析生成

を基本とする。

ユーザーによる再生成機能を設ける場合は、
利用回数を考慮して月額コストを試算する。

月額5,000円の運用予算を超えないモデル・構成を選択する。

---

# 18. UI / デザイン

基本カラー：

- 白
- ネイビー（#1A2E5C）

重視するもの：

- KPIの視認性
- グラフの読みやすさ
- 情報の優先順位
- 操作の分かりやすさ

装飾性より、
短時間で状況を把握できることを優先する。

---

# 19. レスポンシブ・ブラウザ対応

Phase 1ではPCブラウザでの利用を主対象とする。

主要な画面については、
スマートフォンでも大きくレイアウトが崩れない
レスポンシブ対応を行う。

詳細なスマートフォン専用UIは対象外とする。

対象ブラウザは、
主要な最新版Chromium系ブラウザを基本とする。

---

# 20. セキュリティ・データ管理

## 20.1 APIキー

APIキー等の秘密情報を
ソースコードへ直接記載しない。

環境変数またはSecrets管理を使用する。

## 20.2 HTTPS

本番環境はHTTPS通信を使用する。

## 20.3 売上データ

売上データは認証された利用者のみが閲覧できるものとする。

## 20.4 customer_id

`customer_id` は分析に必要な識別子としてのみ使用する。

氏名・住所・電話番号・メールアドレス等の
直接的な個人情報はPhase 1では保存しない。

## 20.5 データ保持

前月比較・リピート率算出に必要なため、
過去の売上データをDBへ保持する。

Phase 1では、
ユーザーによる任意削除機能は必須としない。

データ削除が必要な場合は、
管理者による削除を可能にする。

---

# 21. パフォーマンス

提供されたサンプルCSVは40件である。

Phase 1では、
最大10,000行程度の月次CSVを想定する。

目標として、

CSVアップロード後の検証・集計・ダッシュボード表示を
10秒以内に完了することを目指す。

AI分析処理はこれとは別処理とし、
AI API応答時間を含めない。

---

# 22. コスト要件

## 初期開発予算

45万円以内。

## 月額運用予算

5,000円以内。

対象：

- ホスティング
- データベース
- AI API
- その他インフラ

通常運用として、

- 利用者：約7名
- CSVアップロード：約1回 / 月
- AI分析：約1回 / 月を基本
- 必要に応じて数回の再生成

を想定して試算する。

---

# 23. CI/CD

GitHub Actionsを使用する。

## Pull Request時

最低限以下を自動実行する。

- 型チェック
- Lint
- テスト
- ビルド確認

チェック失敗時は、
原則mainへマージしない。

## mainマージ後

本番環境へ自動デプロイする構成を目指す。

具体的なデプロイ方式は
ARCHITECTURE.mdで決定する。

## AIコードレビュー

Claude Code Headless等を利用した
PR自動レビューは導入候補とする。

コストと必要性を確認して採用を決定する。

---

# 24. Phase分け

## Phase 1

今回の開発対象：

- CSVアップロード
- CSV検証
- DBへのデータ保存
- 月単位でのデータ置換
- 月選択
- 売上
- 粗利
- リピート率
- 前月比較
- 月次推移
- カテゴリ別売上
- SKU別売上TOP10
- 販売数量
- AIサマリー
- AIアクション提案
- ユーザー認証
- ダッシュボードUI
- CI/CD

## Phase 2

将来的な候補：

- Shopify API直接連携
- 日次更新
- 週次分析
- 在庫データ連携
- 在庫回転率
- Amazon連携
- 楽天連携
- 担当営業別分析
- 詳細なロール権限管理
- モバイル専用UI

---

# 25. 受入条件

Phase 1完成時、以下を確認する。

1. 正式フォーマットのCSVをアップロードできる
2. CSV形式・必須列・データ形式のエラーを検出できる
3. 対象月データをDBへ保存できる
4. 同月CSVを再アップロードした場合、既存データを置換できる
5. 売上を正しく算出できる
6. 粗利を正しく算出できる
7. 定義したリピート率を正しく算出できる
8. 前月データがある場合、前月比較を正しく表示できる
9. 前月データがない場合、「比較データなし」と表示できる
10. 過去データがない場合、リピート率を「算出不可」と表示できる
11. カテゴリ別売上を確認できる
12. SKU別売上TOP10を確認できる
13. SKUが10未満の場合、存在するSKUをすべて表示できる
14. 販売数量を確認できる
15. 月次推移を確認できる
16. AIが計算済みデータをもとにサマリーを生成できる
17. AIが次月のアクション案を生成できる
18. 主要KPIとして表示する数値がアプリケーション計算結果と一致する
19. ダッシュボード初期表示で売上・粗利・リピート率を確認できる
20. 主要操作がPCブラウザ上で問題なく利用できる
21. 月額運用費の想定見積りが5,000円以内である
22. 通常の月次報告フローにおいて、手作業での再集計・グラフ作成が不要である

---

# 26. 業務効率化の評価

「3時間 → 30分」はプロジェクトの目標値とし、
システム単体の厳密な受入合否条件とはしない。

評価対象は、

ShopifyからCSVを取得した後、

- CSVアップロード
- ダッシュボード確認
- AI分析確認
- 月次報告内容の確認

までとする。

Excelによる手動集計・グラフ作成が不要になり、
上記作業を30分程度以内で行える状態を目標とする。

---

# 27. 模擬案件上の前提

本案件は模擬案件であるため、
以下はクライアントとの追加ヒアリング結果ではなく、
設計を進めるための仮定として定義する。

- `case8-sales-sample.csv` を正式入力フォーマットとする
- CSVは月ごとにアップロードする
- 過去データはDBへ蓄積する
- 同月再アップロード時は月単位で置換する
- costは行単位の合計原価とする
- revenueは分析に使用可能な確定売上額とする
- 全7名が会社全体データを閲覧する
- 営業担当者別分析は行わない
- 返品・返金処理はCSV側で反映済みとする
- UTF-8 CSVを使用する
- 対象月はCSV内の `order_date` から自動判定する
- 1つのCSVに複数月のデータが含まれている場合はエラーとする
- 過去データの登録も「1ファイル＝1か月」で行い、一括インポート機能はPhase 1では実装しない
- AI分析は対象月ごとに初回1回生成し、その結果を保存して再利用する
- AI分析の再生成機能はPhase 1では実装しない
- 前月データが存在しない場合はAIへ「比較データなし」を明示し、前月比較コメントを生成させない
- 利用者アカウントは管理者が手動で発行し、セルフサインアップ機能はPhase 1では実装しない
- 月内で複数CSVに分割してアップロードする運用はPhase 1では対応しない
- 粗利率は補助KPIとして表示する
- 売上データは集計値だけでなく、CSVの取引行単位でDBへ保存する

実案件化する場合は、
これらをクライアントへ確認したうえで正式要件とする。

---

# 28. ARCHITECTURE.mdで決定する事項

要件ではなく技術設計に属する以下は、
ARCHITECTURE.mdで決定する。

- フロントエンド技術
- バックエンド技術
- データベース
- DBスキーマ
- 認証方式
- ホスティング先
- AIモデル
- AI API
- デプロイ方式
- CSVパースライブラリ
- グラフライブラリ
- AIプロンプト設計
- GitHub Actionsの詳細構成

---

# 29. 開発方針

本案件では、

「実装できる機能を増やすこと」

ではなく、

「意思決定に必要な情報を、
少ない作業で確認できること」

を優先する。

実装判断では以下を基準とする。

1. 月次報告の手作業削減につながるか
2. 売上・粗利・リピート率を分かりやすく確認できるか
3. 前月との変化を簡単に把握できるか
4. 経営・マーケティング・営業の意思決定に役立つか
5. 正式なKPI数値をAI任せにしていないか
6. 現在提供されているデータで実現可能か
7. 月額5,000円以内で継続運用できるか
8. Phase 2の機能を不用意にPhase 1へ追加していないか