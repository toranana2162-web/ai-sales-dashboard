# AI搭載 売上分析ダッシュボード 要件定義書

## 1. プロジェクト概要

### 1.1 プロジェクト名

AI搭載 売上分析ダッシュボード

### 1.2 背景

クライアントはECブランド（アパレル）を運営しており、
毎月Shopifyから売上データをCSV形式でダウンロードし、
Excelを使用して集計・グラフ作成・社内報告を行っている。
現在、この月次報告作業に毎回約3時間かかっている。

作成したレポートは社長・マーケティング部長・営業担当者が閲覧しており、
単なる数値の可視化だけではなく、経営判断や次のアクションにつながる
分かりやすいレポートが求められている。

### 1.3 目的

Shopifyから取得したCSVをアップロードするだけで、
- 売上データの集計
- 重要KPIの表示
- グラフによる可視化
- AIによるデータ分析
- AIによる次月のアクション提案

までを自動化するWebダッシュボードを構築する。
これにより、現在約3時間かかっている月次報告作業を30分程度まで短縮することを目標とする。

---

## 2. プロジェクトのゴール

### ゴール1：月次報告業務の効率化

現在約3時間かかっている月次報告作業を、CSVアップロードを中心とした操作のみで
完了できる状態にし、作業時間を30分程度まで短縮する。

### ゴール2：意思決定に使えるレポートの提供

単にグラフを表示するだけではなく、
- 現在どうなっているのか
- 前月から何が変化したのか
- 次に何をすべきか
を利用者が把握できるダッシュボードとする。

---

## 3. 対象ユーザー

### 主な利用者

- 社長
- マーケティング部長
- 営業担当者 5名
合計7名程度を想定する。

### 利用目的

経営・マーケティング・営業活動に必要な売上情報を確認し、次の施策や意思決定に活用する。

---

## 4. 現在の業務フロー
1. Shopifyから売上データをCSV形式でダウンロード
2. Excelへデータを取り込む
3. 必要なデータを集計
4. グラフを作成
5. レポートを作成
6. 社内へ共有

所要時間：約3時間 / 月

---

## 5. システム導入後の業務フロー

1. Shopifyから売上CSVをダウンロード
2. 本システムへCSVをアップロード
3. CSVの内容を自動検証
4. データを自動集計
5. KPI・グラフを自動生成
6. AIがデータを分析
7. AIが次月のアクションを提案
8. 利用者がダッシュボードを確認

Phase 1ではShopify APIとの直接連携は行わず、CSVアップロード方式とする。

---

# 6. CSVデータ仕様

提供されたサンプルCSVを基準として、

Phase 1では以下の8列を入力データとして使用する。

| 列名 | 内容 | 主な用途 |

|---|---|---|

| order_date | 注文日 | 月次集計・前月比較 |

| customer_id | 顧客ID | リピート率算出 |

| product_name | 商品名 | 商品別分析 |

| category | 商品カテゴリ | カテゴリ別分析 |

| sku | SKU | SKU別ランキング |

| quantity | 販売数量 | 販売数量分析 |

| revenue | 売上金額 | 売上KPI |

| cost | 原価 | 粗利算出 |

### 6.1 CSVアップロード

ユーザーがShopifyから取得したCSVファイルをブラウザ上からアップロードできること。

### 6.2 CSV検証

アップロード時に最低限以下を確認する。
- CSV形式であること
- 必須8列が存在すること
- `order_date` が有効な日付であること
- `quantity` が有効な数値であること
- `revenue` が有効な数値であること
- `cost` が有効な数値であること
- データが空でないこと

検証に失敗した場合は、どの項目に問題があるのかユーザーが理解できるエラーメッセージを表示する。

---

# 7. KPI要件

## 7.1 最重要KPI

クライアントが毎月の経営会議で必ず確認する

以下3つを最重要KPIとする。

1. 売上
2. 粗利
3. リピート率
ダッシュボードの視認性の高い位置に表示する。

### 7.1.1 売上

CSVの `revenue` を対象期間ごとに合計して算出する。

計算式：
売上 = SUM(revenue)

### 7.1.2 粗利

CSVの `revenue` と `cost` を使用して算出する。

計算式：

粗利 = SUM(revenue - cost)

必要に応じて粗利率も補助情報として表示できるものとする。

粗利率 = 粗利 ÷ 売上 × 100

### 7.1.3 リピート率

CSVには `customer_id` が存在するため、
同一顧客の購入履歴を利用してリピート判定を行うことができる。

ただし、リピート率には複数の定義が存在するため、
Phase 1では以下の定義を採用候補とする。

「対象月に購入した顧客のうち、対象月より前にも購入履歴が存在する顧客の割合」

計算式：

リピート率 =対象月のリピーター顧客数÷対象月のユニーク購入顧客数× 100
この定義については実装前の最終レビューで確定する。

---

## 7.2 補助分析

以下を補助的な分析情報として表示する。
- カテゴリ別売上
- SKU別売上ランキング
- 販売数量
- 前月比較

### カテゴリ別分析

`category` を使用して、カテゴリごとの売上や構成を確認できるようにする。

### SKU別分析

`sku` を使用して売上を集計し、SKU別売上ランキングを表示する。
要件上はTOP10を表示する。

---

## 7.3 在庫回転率

クライアントから「あったら嬉しいKPI」として在庫回転率の要望がある。

しかし、現在提供されている売上CSVには、
- 現在庫数
- 期首・期末在庫
- 平均在庫数量
- 平均在庫金額

などの在庫データが含まれていない。

そのため、現在のCSVのみでは在庫回転率を正確に算出することはできない。

Phase 1で在庫回転率を実装する場合は、別途在庫データの提供方法を決定する必要がある。
現時点では「データ不足により保留」とし、現在のCSVから推測値を生成しない。

---

## 7.4 表示期間

Phase 1では月次データを対象とする。

CSV内の `order_date` を利用して月単位に集計する。
日次・週次分析についてはPhase 2での対応候補とする。

---

# 8. ダッシュボード機能

## 8.1 KPI表示

以下3つの最重要KPIを

ダッシュボード上部など視認性の高い位置に表示する。
- 売上
- 粗利
- リピート率
可能な場合は前月値および前月比も表示する。

## 8.2 グラフ・ランキング

Phase 1では最低限以下を確認できること。
- 月次売上推移
- 月次粗利推移
- リピート率
- カテゴリ別売上
- SKU別売上TOP10
- 販売数量
- 前月比較

具体的なグラフ形式については、データ特性と既存Excelレポートを確認したうえで決定する。

---

# 9. AI分析機能

## 9.1 目的

AIは単に数値を文章化するのではなく、利用者の意思決定を支援する目的で使用する。

## 9.2 AIへ渡すデータ

KPIの数値計算そのものをAIへ任せない。
アプリケーション側で計算した、
- 売上
- 粗利
- リピート率
- 前月比
- カテゴリ別集計
- SKU別集計
- 販売数量
などの構造化された分析結果をAIへ渡す。

## 9.3 分析内容

AIは最低限以下についてコメントする。
- 当月の状況サマリー
- 前月との比較
- 大きく変化したKPI
- 売上への寄与が大きいカテゴリ・商品
- 注目すべき変化
- 次月のアクション提案

## 9.4 トーン

クライアント要望に基づき、コンサルタントが分析・提案しているようなトーンとする。
ただし、データから確認できない原因を事実として断定してはならない。

## 9.5 ハルシネーション対策

AIは入力データに存在しない数値や事実を生成してはならない。
数値計算はアプリケーション側で行い、AIは計算済みデータの解釈を担当する。

原因を推測する場合は、
「可能性があります」
「考えられます」
など、事実と推測を区別できる表現とする。

---

# 10. デザイン要件

## 基本カラー
- 白
- ネイビー（#1A2E5C）

## 方針

社長・マーケティング部長・営業担当者が短時間で重要な情報を把握できるデザインとする。
装飾性よりも、
- 視認性
- KPIの分かりやすさ
- グラフの読みやすさ
- 情報の優先順位
を重視する。
既存のブランドガイドラインおよびロゴが提供された場合は、それらを反映する。

---

# 11. コスト要件

## 初期開発予算

45万円以内

## 月額運用予算

5,000円以内

対象：
- ホスティング
- データベース
- AI API
- その他インフラ費用

技術選定時には月額5,000円以内に収まる構成を優先する。

---

# 12. Phase分け

## Phase 1：今回の開発対象
- Shopify CSVアップロード
- CSV検証
- データ集計
- 月次ダッシュボード
- 売上KPI
- 粗利KPI
- リピート率KPI
- 前月比較
- カテゴリ別分析
- SKU別売上TOP10
- 販売数量分析
- AI現状サマリー
- AIによる次月アクション提案
- 白＋ネイビーを基調としたUI

## Phase 1：保留事項

### 在庫回転率

現在提供されているCSVのみでは算出できないため、
必要な在庫データの提供方法が決まるまで保留とする。

## Phase 2：将来的な拡張候補
- Shopify API直接連携
- 日次データ更新
- 週次分析
- 在庫データ連携
- 在庫回転率
- Amazonデータ連携
- 楽天データ連携
- その他ECモールとの統合
Phase 2の機能は今回の実装範囲に含めない。

---

# 13. 非機能要件

## 13.1 セキュリティ
- APIキーをソースコードへ直接記載しない
- 環境変数またはSecrets管理を使用する
- 売上データを不必要に外部公開しない
- 本番環境ではHTTPS通信を使用する

## 13.2 パフォーマンス
一般的な月次Shopify CSVをアップロードした際、
実用上問題のない時間で集計・表示が完了すること。
具体的な最大CSVサイズ・最大行数については実運用データを確認後に確定する。

## 13.3 エラー処理

以下の場合に適切なエラーを表示する。
- CSV形式が不正
- 必須列が不足
- 日付形式が不正
- 数値形式が不正
- データが空
- AI APIエラー
- サーバーエラー

ユーザーが次に何をすればよいか分かるエラーメッセージを表示する。

---

# 14. CI/CD
GitHub Actionsを使用し、コード変更時の品質確認を自動化する。

## Pull Request時

最低限以下を自動実行する。
- 型チェック
- Lint
- テスト
- ビルド確認

チェックに失敗した場合は、原則としてmainへマージしない。

## mainマージ後

本番環境への自動デプロイを行う構成とする。
具体的なデプロイ方式はARCHITECTURE.mdで決定する。

## AIコードレビュー

Claude Code Headless等を利用したPR自動レビューについても導入候補とする。
実装方法・コスト・必要性を確認したうえで採用を判断する。

---

# 15. 受入条件

Phase 1完成時、最低限以下を満たすこと。

1. 指定された8列を持つCSVをアップロードできる
2. 不正なCSVを検出し、原因を表示できる
3. `revenue` から月次売上を正しく算出できる
4. `revenue` と `cost` から粗利を正しく算出できる
5. `customer_id` の購入履歴から定義済みのリピート率を算出できる
6. 前月とのKPI比較ができる
7. カテゴリ別売上を確認できる
8. SKU別売上TOP10を確認できる
9. 販売数量を確認できる
10. 月次データをグラフで確認できる
11. AIが計算済みデータに基づいたサマリーを生成できる
12. AIが次月のアクション案を生成できる
13. AIが入力されていない数値を事実として生成しない
14. 社長・マーケティング部長・営業担当者が主要KPIを容易に確認できる
15. 月次報告作業を従来の約3時間から30分程度まで短縮できる
16. 月額運用コストを5,000円以内に収められる

在庫回転率については、必要な在庫データが提供されない限りPhase 1の受入条件には含めない。

---

# 16. 未確定事項
実装開始前に以下を確認・確定する。

## 業務定義
- リピート率の正式な定義
- 粗利計算に `cost` をそのまま使用してよいか
- 前月比較の表示方法
- 過去データの保持期間

## 在庫回転率
- Phase 1で本当に必要か
- 必要な場合、在庫データをどこから取得するか
- 在庫CSVを別途用意するか
- Shopify API等から取得するか

## UI
- 既存Excelレポート
- ブランドガイドライン
- ロゴデータ
- 各グラフの詳細形式

## AI
- 使用するAIモデル
- AI分析結果を保存するか
- 1回あたりの分析対象範囲

## インフラ
- ホスティング先
- データベース
- 認証方式
- AI API
- 本番環境構成

これらの技術選定については
ARCHITECTURE.mdで確定する。

---

# 17. 開発方針
本案件では、「作れる機能を増やすこと」ではなく、
「クライアントが意思決定に必要な情報を、少ない作業で確認できること」を優先する。

以下を設計・実装判断の基準とする。

1. 月次報告作業の3時間 → 30分への短縮につながるか
2. 売上・粗利・リピート率を分かりやすく確認できるか
3. 経営・マーケティング・営業の意思決定に役立つか
4. AIに計算を任せず、信頼できる数値を提示できるか
5. 月額5,000円以内で継続運用できるか
6. 現在提供されているデータで実現可能か
7. Phase 2の機能をPhase 1へ不用意に持ち込んでいないか