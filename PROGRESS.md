# 実装進捗メモ

最終更新: 2026-09-17

このファイルは「今どこまで進んでいて、次に何をするか」を記録するための作業メモです。
正式な仕様はREQUIREMENTSv2.md、設計はARCHITECTURE.md、作業リストはTASKS.md、
日付ごとの記録はDEVLOG.mdを参照してください。

---

## 今の状態（ひとことで）

**Milestone 0〜4（プロジェクト基盤・DB・認証・CSVアップロード・KPI集計API）がすべて完了。** Milestone 5（ダッシュボードUI）に着手する段階。

---

## 完了したこと

### Milestone 0〜3：省略（DEVLOG.md参照）

### Milestone 4: KPI集計API（すべて完了）
- `lib/kpi/calculations.ts`：リピート率・粗利率・前月計算（純粋関数、6件テスト）
- `lib/kpi/rpc.ts`：5つのRPC呼び出しのラッパー関数＋補助関数（`hasSalesBefore`, `monthHasData`）、10件テスト
- `GET /api/months`：データが存在する対象月の一覧
- `GET /api/kpi?month=YYYY-MM`：売上・粗利・粗利率・リピート率・前月比較・カテゴリ別・SKU別TOP10
- `GET /api/kpi/trend?month=YYYY-MM&months=12`：複数月の推移

**動作確認済みの内容（ローカル環境で実際にブラウザから確認）**：
- `/api/kpi?month=2025-11`：CSVファイルの実際の合計値（売上264,700円・粗利163,100円・数量21）と完全に一致
- リピート率「算出不可」（11月より前のデータが無い）、「比較データなし」（10月分が未アップロード）が正しく表示
- `/api/kpi/trend?month=2025-12`：11月(算出不可)・12月(0%、算出不可ではない)の違いが正しく区別されることを確認
  - 11月の実データアップロードにより、Milestone 1のダミー客(C999)の11月購入履歴が消えたことで、12月が「初回購入」扱いになり repeat_rate:0 になる、という正しい仕組みの再確認ができた

**発生した小さな不具合と対応**：`proxy.ts`のリダイレクト処理で、`/login`へ飛ばす際に元のURLのクエリ文字列(`?month=...`)が残ってしまう不具合を発見・修正。

---

## 次にやること：Milestone 5（ダッシュボードUI）

- [ ] 対象月選択UIを実装する（`/api/months`から取得）
- [ ] 最重要KPIカード（売上・粗利・リピート率、前月比）を表示する
- [ ] 月次売上推移・月次粗利推移・リピート率推移グラフを実装する（Recharts）
- [ ] カテゴリ別売上グラフを実装する
- [ ] SKU別売上TOP10テーブル/グラフを実装する
- [ ] 販売数量表示を実装する
- [ ] レスポンシブ対応を確認する
- [ ] 主要な最新版Chromium系ブラウザでの動作確認

このMilestoneから`Recharts`（グラフ描画ライブラリ）を導入する。今の仮のアップロード画面(`app/page.tsx`)を、本来のダッシュボード画面に置き換えていく。

---

## 注意点・思い出しておくこと

- 進め方のルール：各タスクの前に「何を・なぜ・どこにつながるか・用語」を日本語で説明してから実装し、実装後は変更内容と確認結果を報告する
- GitHub・Supabase・Vercel・APIキーなど、外部サービスに関わる操作はAIが勝手に進めず、ユーザーへの手順案内という形で進める
- TASKS.md、ARCHITECTURE.md、REQUIREMENTSv2.mdに無い機能は追加しない
- 章番号の引用は「REQ」（REQUIREMENTSv2.md）「ARCH」（ARCHITECTURE.md）を付けて区別している
- 秘密情報（Secret key等）はチャットに貼らず、ユーザー自身がファイルへ直接入力する運用にしている
- テストは`npm test`（Vitest）で実行。CIへの組み込みはMilestone 7で予定通り実施
- 開発サーバーはポートが3000/3001と変わることがある（他のプロセスの状況次第）。ブラウザで開くURLのポート番号に注意
- `tmp-test-data/`はGit管理対象外の一時テストファイル置き場
- 現在DBに入っている実データ：2025年11月(15件、正式サンプルCSVより)、2025年12月(C999さんのダミー1件、Milestone1のテスト時に投入)
